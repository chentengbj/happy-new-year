# 网站部署指南

## 当前状态

目前网站是**本地文件**（使用 `file://` 协议），只能在本机访问。要让别人访问，需要部署到服务器。

## 部署方案

### 方案1：本地服务器（适合局域网访问）

#### 使用 Python 简单服务器

```bash
# 进入项目目录
cd "/Users/chenteng/Desktop/cursor 启动/code"

# Python 3
python3 -m http.server 8000

# 或者指定IP（让局域网访问）
python3 -m http.server 8000 --bind 0.0.0.0
```

**访问方式：**
- 本机访问：`http://localhost:8000`
- 局域网访问：`http://你的IP地址:8000`（例如：`http://192.168.1.100:8000`）

**查看本机IP：**
```bash
# macOS
ifconfig | grep "inet " | grep -v 127.0.0.1

# 或者
ipconfig getifaddr en0
```

#### 使用 Node.js 服务器

```bash
# 安装 http-server（如果还没安装）
npm install -g http-server

# 启动服务器
cd "/Users/chenteng/Desktop/cursor 启动/code"
http-server -p 8000 -a 0.0.0.0
```

### 方案2：GitHub Pages（免费，适合公开访问）

#### 步骤：

1. **创建 GitHub 仓库**
   ```bash
   cd "/Users/chenteng/Desktop/cursor 启动"
   git init
   git add code/
   git commit -m "Initial commit"
   ```

2. **在 GitHub 上创建新仓库**（例如：`education-tracking`）

3. **推送代码**
   ```bash
   git remote add origin https://github.com/你的用户名/education-tracking.git
   git branch -M main
   git push -u origin main
   ```

4. **启用 GitHub Pages**
   - 进入仓库 Settings → Pages
   - Source 选择 `main` 分支，文件夹选择 `/code` 或 `/root`
   - 保存后，网站地址为：`https://你的用户名.github.io/education-tracking/`

**注意：** 如果仓库名不是 `你的用户名.github.io`，访问路径需要包含仓库名。

### 方案3：Netlify（免费，简单）

1. **访问** https://www.netlify.com
2. **注册/登录**账号
3. **拖拽** `code` 文件夹到 Netlify 部署区域
4. **自动获得**一个免费域名（例如：`your-site.netlify.app`）

### 方案4：Vercel（免费，快速）

1. **安装 Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **部署**
   ```bash
   cd "/Users/chenteng/Desktop/cursor 启动/code"
   vercel
   ```

3. **按提示操作**，会自动获得一个免费域名

### 方案5：云服务器（适合长期使用）

如果已有云服务器（阿里云、腾讯云、AWS等）：

1. **上传文件到服务器**
   ```bash
   # 使用 scp
   scp -r code/ user@your-server-ip:/var/www/html/
   ```

2. **配置 Web 服务器**（Nginx 或 Apache）

3. **访问**：`http://your-server-ip` 或配置域名

## 推荐方案

- **快速测试/局域网访问**：使用 Python 简单服务器（方案1）
- **公开访问/免费托管**：GitHub Pages（方案2）或 Netlify（方案3）
- **专业部署**：云服务器（方案5）

## 注意事项

1. **文件大小**：`data.js` 文件约 419KB，GitHub Pages 等免费服务通常支持
2. **更新数据**：如果使用自动化脚本更新数据，需要：
   - 本地服务器：手动运行脚本后重启服务器
   - GitHub Pages：推送更新后自动部署
   - 其他服务：根据服务商说明更新
3. **安全性**：如果包含敏感信息，建议使用私有仓库或添加访问控制

## 快速启动本地服务器脚本

创建一个启动脚本 `start_server.sh`：

```bash
#!/bin/bash
cd "/Users/chenteng/Desktop/cursor 启动/code"
echo "🚀 启动本地服务器..."
echo "📡 本机访问: http://localhost:8000"
echo "🌐 局域网访问: http://$(ipconfig getifaddr en0 2>/dev/null || hostname -I | awk '{print $1}'):8000"
echo ""
python3 -m http.server 8000 --bind 0.0.0.0
```

使用方法：
```bash
chmod +x start_server.sh
./start_server.sh
```

