# 🚀 部署步骤 - 按顺序执行

## 第一步：检查 Git 状态

```bash
cd "/Users/chenteng/Desktop/cursor 启动/code"
git status
```

如果显示 "not a git repository"，需要初始化。

## 第二步：初始化 Git 仓库

```bash
git init
```

## 第三步：添加所有文件

```bash
git add .
```

## 第四步：提交文件

```bash
git commit -m "Initial commit: 教培行业动向追踪系统"
```

## 第五步：在 GitHub 上创建仓库

1. 访问 https://github.com
2. 登录（如果没有账号，先注册）
3. 点击右上角 "+" → "New repository"
4. 填写：
   - Repository name: `education-tracking`
   - Description: `教培行业动向追踪系统`
   - 选择 **Public**（必须公开才能使用免费 GitHub Pages）
   - **不要**勾选 "Initialize with README"
5. 点击 "Create repository"

## 第六步：连接远程仓库

**重要：** 将 `YOUR_USERNAME` 替换为你的 GitHub 用户名！

```bash
git remote add origin https://github.com/YOUR_USERNAME/education-tracking.git
git branch -M main
```

## 第七步：推送到 GitHub

```bash
git push -u origin main
```

**注意：** 首次推送需要输入 GitHub 用户名和密码（或 Personal Access Token）

如果提示需要认证，可以：
- 使用 Personal Access Token（推荐）
- 或使用 GitHub CLI

## 第八步：启用 GitHub Pages

1. 在 GitHub 仓库页面，点击 "Settings"
2. 左侧菜单找到 "Pages"
3. 配置：
   - Source: "Deploy from a branch"
   - Branch: `main`
   - Folder: `/ (root)`
4. 点击 "Save"

## 第九步：等待部署

- GitHub 会在几分钟内自动部署
- 在仓库页面可以看到部署状态
- 部署完成后，网站地址：`https://YOUR_USERNAME.github.io/education-tracking/`

---

## 🔧 如果遇到问题

### 问题1：需要认证
**解决方案：** 使用 Personal Access Token
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. 选择权限：`repo`（全部）
4. 生成后复制 token
5. 推送时，密码输入 token

### 问题2：文件太大
**解决方案：** 检查 `.gitignore` 是否正确配置

### 问题3：推送被拒绝
**解决方案：** 
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

